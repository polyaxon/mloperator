# This rule is not used by the project mloperator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the polyaxon.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: mloperator
    app.kubernetes.io/managed-by: kustomize
  name: operation-editor-role
rules:
- apiGroups:
  - polyaxon.com
  resources:
  - clusters
  - jobs
  - kfjobs
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - polyaxon.com
  resources:
  - clusters/finalizers
  - jobs/finalizers
  - kfjobs/finalizers
  - services/finalizers
  verbs:
  - get
